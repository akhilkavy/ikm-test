import React from 'react';
import { Table } from 'react-bootstrap';

function Districts(props) {
  return (
    
        <option id={props.id} name={props.name}>{props.name}</option>
        
    
  )
}

export default Districts